// import { fromJS } from 'immutable';
// import { selectTutorSignedInLandingPageDomain } from '../selectors';

describe('selectTutorSignedInLandingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
