// import { fromJS } from 'immutable';
// import { selectStudentSignedInLandingPageDomain } from '../selectors';

describe('selectStudentSignedInLandingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
